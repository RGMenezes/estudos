let nomeVariavel:boolean = false;

//boolean é o type, já Boolean é o objeto. boolean != Boolean

console.log(`Temos o tipo boolean: ${nomeVariavel}, e seu oposto ${!nomeVariavel}. typeof = ${typeof nomeVariavel}`);